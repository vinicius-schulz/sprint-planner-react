version: "3.8"

services:
  mongo:
    image: mongo:7
    container_name: sprint_planner_mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: sprint_planner
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
